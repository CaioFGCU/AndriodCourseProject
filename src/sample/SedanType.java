package sample;

/**
 * enum to get type of sedan. type can determine number of doors, or different models
 * with different properties.
 */
public enum SedanType {
    SPORT,
    MUSCLE,
    COUPE,
    FOUR_DOOR
}
