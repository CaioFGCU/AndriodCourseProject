package sample;

/**
 * Enum class to determine the color of indicated vehicles
 */
public enum VehicleColor {
    Red,
    Blue,
    Green,
    Yellow,
    Black,
    Silver,
    Orange,
    Pink,
    White
}